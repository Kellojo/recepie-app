# PocketBase Configuration
# Generate a secure 32-character encryption key for production
PB_ENCRYPTION_KEY=your-32-char-encryption-key-here

# Optional: Set custom port (default: 8080)
# PORT=8080

# Optional: Set custom host (default: 0.0.0.0)
# HOST=0.0.0.0